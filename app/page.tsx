import { redirect } from 'next/navigation';

export default function RootPage() {
  // This file is now redundant as app/(public)/page.tsx handles the root path.
  // We keep it as a simple passthrough or can be deleted manually.
  return null;
}